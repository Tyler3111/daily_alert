"""Job payload normalization placeholder."""

from typing import Any


async def normalize_jobs(payload: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """Normalize raw job payloads into a consistent structure."""

    return payload
